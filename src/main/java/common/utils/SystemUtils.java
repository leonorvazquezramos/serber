package common.utils;

import common.annotations.DocumentedClass;
import logic.system.Instant;

@DocumentedClass(
        description = "static procedures to use across the whole application"
)
public class SystemUtils {
    public static Instant current() {
        return
    }
}
